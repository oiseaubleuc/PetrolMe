# Deploy OMS on Netlify site (e.g. petroloms.netlify.app)
# Base directory points at the Next.js app, not the marketing HTML.

[build]
  base = "petrolisation-oms"
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "20"
  NEXT_TELEMETRY_DISABLED = "1"
